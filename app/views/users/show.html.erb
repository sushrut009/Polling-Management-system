<% provide(:title, @user.name) %>
<div class="row">
  <aside class="col-md-4">
    <section class="user_info">
      <h1>
        <%= gravatar_for @user %>
        <h3><%= @user.name %> </h3>
        <h4>Admin ID: <%=  @user.id %></h4>
      </h1>
    </section>
  </aside>
</div>
<li><%= link_to 'Add poll', new_user_poll_path(params[:id]), class: 'btn btn-default' %></li>
<li><%= link_to 'List of Polls', poll_index1_path(params[:id]), class: 'btn btn-default' %></li>
</div>
</div>
